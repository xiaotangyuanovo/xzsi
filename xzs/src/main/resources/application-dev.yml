# 日志输出到项目根目录的 log/ 目录
logging:
  file:
    path: ./log
  level:
    root: info
    # 如果排错需要更详细日志，可临时开 debug
    # com.xzs: debug
    # org.mybatis: debug

server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/xzs?useSSL=false&useUnicode=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=true&allowMultiQueries=true
    username: root
    password: ty20010701
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      max-lifetime: 1800000

# MyBatis 映射文件位置（你的项目 resources/mapper 下）
mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
